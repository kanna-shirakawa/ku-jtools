#!/bin/bash
#
# KUBiC Labs package install file
# based on template 1.3 (2012/10)

set -e

. ku/install-functions.sh

[ "x$1" == "xmake_controls" ] && {
	make_control_files
	rm -f $tempinstall
	exit 0
}

check_env || exit 1

install_files

# add custom tasks here

exit 0
