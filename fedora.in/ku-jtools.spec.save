# -*- coding: utf-8 -*-
%ifos linux
%define _bindir /bin
%endif

Summary: __description__
Name: ku-__TOOLKIT__
Version: __TOOLKIT_VERSION__
Release: __TOOLKIT_RELEASE__
License: LGPL2
URL: __homepage__
Source0: http://www.kubiclabs.com/sources/ku-__TOOLKIT_____TOOLKIT_VERSION__-__drelease__.tar.gz
BuildRoot: %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)

%description
A small set of tools for projects development and management:
.
 - project directory structure maintenance
 - shell environment auto setup based on concept of 'current project'
 - debian packages build and repo maintenance utils/frontends

%prep
%setup -q

%build
make build

%check

%install
rm -rf ${RPM_BUILD_ROOT}
fakeroot make DESTDIR=$RPM_BUILD_ROOT install

%post

%preun

%clean
rm -rf ${RPM_BUILD_ROOT}

%files
%defattr(-,root,root,-)
/

%changelog
* __rtoday__ __maintainer__
- mod: changed references from kl-base -> ku-base
- fix: install-functions.sh, chown error in make_control_files()
- new: bin/jtupdate-standards, to auto update ku build standard files

* Mon Oct 22 2012 KUBiC Labs - Lorenzo Canovi <lorenzo@kubiclabs.com>
- fix: jtcj, improved parsing when using command (now uses all parms
  after prjname as command, not only the first one)
- mod: moved install helpers (install, install-functions.sh, install.sh)
  into subdir "ku"
- mod: jtdeb-rebuild now search for "ku/makefile-test", "ku/makefile" and
  "ku/Makefile" to preprocess .in files, instead of Makefile, so you can
  patch original packages maintaining the pristine structure unmodified;
  note that jtdeb-make still uses Makefile (calls dpkg-rebuildpackage), so
  you cannot use ku/Makefile to install things
- new: copy of install helpers in /usr/lib/__TOOLKIT__/install dir

* Fri Oct 12 2012 KUBiC Labs - Lorenzo Canovi <lorenzo@kubiclabs.com>
- new: tools for scm (fossil) management
- add: jtfos-* and jtscm-* commands

* Thu Sep 13 2012 KUBiC Labs - Lorenzo Canovi <lorenzo@kubiclabs.com>
- mod: repackaged for multi distro support (debian, fedora)
- mod: jtdeb-rebuild, debian_control -> control
- mod: jtdir, added support for fossil scm (and --fossil option)
- fix: jtdir, --bazaar check was flawed
